<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BrainTrainerApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <item name="android:fontFamily">@font/righteous</item>
        <item name="fontFamily">@font/righteous</item>
        <item name="android:textColor">#FFFFFF</item>
        
        <!-- Shadow effect -->
        <item name="android:shadowColor">#000000</item>
        <item name="android:shadowDx">0</item>
        <item name="android:shadowDy">6</item>
        <item name="android:shadowRadius">1</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        
        <item name="buttonStyle">@style/ClashButtonStyle</item>
        
        <!-- Window Animations -->
        <item name="android:windowAnimationStyle">@style/WindowAnimation.Fade</item>
    </style>

    <style name="WindowAnimation.Fade" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/fade_in</item>
        <item name="android:activityOpenExitAnimation">@anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/fade_out</item>
    </style>

    <style name="ClashButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:fontFamily">@font/righteous</item>
        <item name="fontFamily">@font/righteous</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>
